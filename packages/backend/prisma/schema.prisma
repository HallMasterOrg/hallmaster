generator client {
    provider     = "prisma-client"
    output       = "../src/prisma/generated"
    moduleFormat = "esm"
}

datasource db {
    provider = "postgresql"
}

enum Hypervisor {
    DOCKER
    RAW
}

model Bot {
    id            String     @id
    clusterNumber Int
    shards        Int
    sourceCode    String?
    hypervisor    Hypervisor
    clusters      Cluster[]
}

enum ClusterStatus {
    STARTING
    RUNNING
    STOPPED
    ERROR
}

model Cluster {
    id    String @id @default(uuid())
    botId String

    /// Either a Process ID (PID) or a Docker container ID
    handleId String?

    status ClusterStatus

    shardIds Int[]

    bot Bot @relation(fields: [botId], references: [id], onDelete: Cascade)
}
